from distutils.core import setup


setup(name='tool',
      description='Tool to live-migrate docker containers',
      license='GPLv2',
      packages=['tool'],
      )
setup(name='server',
      description='Tool to live-migrate docker containers',
      license='GPLv2',
      packages=['server'],
      )
setup(name='connect',
      description='Tool to live-migrate docker containers',
      license='GPLv2',
      packages=['connect'],
      )